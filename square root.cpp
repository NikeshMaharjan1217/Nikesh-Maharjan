#include<stdio.h>
#include<conio.h>
#include<math.h>
main()
{
	float a,s;
	printf("Enter the value of a:");
	scanf("%f",&a);
	s=sqrt(a);
	printf("Square root of a is %.2f",s);
	getch();
}
